# roaringbitmap_hive_udf
A Hive/Spark UDF implementation for RoaringBitmap that optimizes exact distinct counting, user cohort analysis, and set operations (intersections, unions) for big data workflows.
